import {color} from './editor';
import {tokenColors, semanticHighlighting, semanticTokenColors} from './tokens';

const name = "Insomnia Dark";

export const theme = {
    name: name,
    colors: color,
    tokenColors: tokenColors,
    semanticHighlighting: semanticHighlighting,
    semanticTokenColors: semanticTokenColors
}